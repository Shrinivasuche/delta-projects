<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chat</title>
</head>
<body>
    <h3>Edit your chat: <%=chat.id%></h3>
    <p>this chata was sent by <b><%=chat.from%></b> to <b><%=chat.to%></b></p>
    <form method="POST" action="/chats/<%=chat.id%>?_method=PUT">
        <textarea name="msg" rows="5" cols="20"><%=chat.msg%></textarea>
        <button>edit</button>
    </form>
</body>
</html>